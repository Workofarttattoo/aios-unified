"""Stub for aios.costs — cost estimation helpers."""
import logging as _logging
_LOG = _logging.getLogger(__name__)

def estimate_cost_usd(model: str = "", tokens: int = 0, **kwargs) -> float:
    """Estimate API call cost in USD. Returns 0.0 as stub."""
    return 0.0
